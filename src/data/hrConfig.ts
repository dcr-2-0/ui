/**
 * HR contact configuration — DEFAULT/FALLBACK only.
 * The live value is managed by admins in the dashboard Settings tab and
 * stored in Firestore (appConfig/main.hrEmail); see useHrEmail(). This
 * constant is used only when no value has been configured there.
 */
export const HR_EMAIL = 'yana.tyshler@develeap.com';
